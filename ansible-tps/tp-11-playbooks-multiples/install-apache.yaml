---
# Playbook secondaire autonome
- name: install apache web server
  hosts: webservers
  become: true
  tasks:
    - name: install apache2
      apt:
        name: apache2
        state: latest
    - name: start apache2
      service:
        name: apache2
        state: started
        enabled: yes
